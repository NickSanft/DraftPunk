{
  "name": "draft-punk",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace-level scripts for the Draft Punk monorepo (frontend in app/, sync server in server/).",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@playwright/test": "^1.50.0",
    "@types/node": "^20.0.0"
  }
}
